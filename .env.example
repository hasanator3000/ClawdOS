# App
APP_URL=http://localhost:3000
SESSION_PASSWORD=change_me_to_a_long_random_string_at_least_32_chars
# SESSION_COOKIE_SECURE=true  # Uncomment if behind HTTPS

# Database
DATABASE_URL=postgres://lifeos:lifeos_change_me@127.0.0.1:5432/lifeos

# Clawdbot (AI agent gateway)
CLAWDBOT_URL=http://127.0.0.1:18789
CLAWDBOT_TOKEN=change_me_generate_with_openssl_rand_hex_24
LIFEOS_CONSULT_TOKEN=change_me_generate_with_openssl_rand_hex_24

# Telegram (optional — needed for 2FA codes and password recovery)
# Get token from @BotFather on Telegram
# TELEGRAM_BOT_TOKEN=

# Optional: gate access when exposing by IP (stored in cookie after first entry)
# ACCESS_TOKEN=change_me_to_a_long_random_token
